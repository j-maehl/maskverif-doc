# --------------------------------------------------------------------
true : use_menhir, menhir_explain, menhir_table
true : warn_Z, warn_Y
true : -traverse

# --------------------------------------------------------------------
<src>   : include
<src/*> : package(zarith,unix)

<src/main.byte>   : linkdep(src/shrcnt_low.o), custom
<src/main.native> : linkdep(src/shrcnt_low.o)
