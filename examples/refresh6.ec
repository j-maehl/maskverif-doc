require import Byte.
module R = {
  proc refresh7 (a1 a2 a3 a4 a5 a6 a7:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a7 = a7 + r;

    r = $distr;
    a3 = a3 + r;
    a7 = a7 + r;

    r = $distr;
    a6 = a6 + r;
    a7 = a7 + r;

    r = $distr;
    a4 = a4 + r;
    a6 = a6 + r;

    r = $distr;
    a2 = a2 + r;
    a5 = a5 + r;

    return (a1, a2, a3, a4, a5, a6, a7);
  }
}.

masking sni 6 R.refresh7 Byte.ComRing.(+).


module R = {
  proc refresh7 (a1 a2 a3 a4 a5 a6 a7:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a6 = a6 + r;

    r = $distr;
    a2 = a2 + r;
    a3 = a3 + r;

    r = $distr;
    a2 = a2 + r;
    a7 = a7 + r;

    r = $distr;
    a4 = a4 + r;
    a7 = a7 + r;

    r = $distr;
    a5 = a5 + r;
    a6 = a6 + r;

    return (a1, a2, a3, a4, a5, a6, a7);
  }
}.
masking sni 6 R.refresh7 Byte.ComRing.(+).


module R = {
  proc refresh7 (a1 a2 a3 a4 a5 a6 a7:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a7 = a7 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a2 = a2 + r;
    a6 = a6 + r;

    r = $distr;
    a2 = a2 + r;
    a3 = a3 + r;

    r = $distr;
    a2 = a2 + r;
    a7 = a7 + r;

    r = $distr;
    a4 = a4 + r;
    a7 = a7 + r;

    r = $distr;
    a5 = a5 + r;
    a6 = a6 + r;

    return (a1, a2, a3, a4, a5, a6, a7);
  }
}.

masking sni 6 R.refresh7 Byte.ComRing.(+).

module R = {
  proc refresh5 (a1 a2 a3 a4 a5:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a4 = a4 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a2 = a2 + r;
    a5 = a5 + r;

    r = $distr;
    a3 = a3 + r;
    a5 = a5 + r;

    return (a1, a2, a3, a4, a5);
  }
}.

masking sni 4 R.refresh5 Byte.ComRing.(+).


module R = {
  proc refresh3 (a1 a2 a3:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    return (a1, a2, a3);
  }
}.

masking sni 2 R.refresh3 Byte.ComRing.(+).


module R = {
  proc refresh7 (a1 a2 a3 a4 a5 a6 a7:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a3 = a3 + r;

    r = $distr;
    a2 = a2 + r;
    a6 = a6 + r;

    r = $distr;
    a4 = a4 + r;
    a6 = a6 + r;

    r = $distr;
    a5 = a5 + r;
    a7 = a7 + r;

    return (a1, a2, a3, a4, a5, a6, a7);
  }
}.

masking sni 6 R.refresh7 Byte.ComRing.(+).



module R = {
  proc refresh7 (a1 a2 a3 a4 a5 a6 a7:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a3 = a3 + r;

    r = $distr;
    a2 = a2 + r;
    a7 = a7 + r;

    r = $distr;
    a2 = a2 + r;
    a4 = a4 + r;

    r = $distr;
    a2 = a2 + r;
    a6 = a6 + r;

    r = $distr;
    a3 = a3 + r;
    a6 = a6 + r;

    r = $distr;
    a5 = a5 + r;
    a7 = a7 + r;

    return (a1, a2, a3, a4, a5, a6, a7);
  }
}.

masking sni 6 R.refresh7 Byte.ComRing.(+).


module R = {
  proc refresh6 (a1 a2 a3 a4 a5 a6:byte) = {
    var r:byte;
  
 r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a3 = a3 + r;
    a4 = a4 + r;

    r = $distr;
    a5 = a5 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a1 = a1 + r;
    a6 = a6 + r;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a4 = a4 + r;
    a5 = a5 + r;

    return (a1, a2, a3, a4, a5, a6);
  }

proc refresh5 (a1 a2 a3 a4 a5:byte) = {
    var r:byte;

    r = $distr;
    a1 = a1 + r;
    a2 = a2 + r;

    r = $distr;
    a1 = a1 + r;
    a3 = a3 + r;

    r = $distr;
    a4 = a4 + r;
    a5 = a5 + r;

    r = $distr;
    a1 = a1 + r;
    a4 = a4 + r;

    r = $distr;
    a3 = a3 + r;
    a5 = a5 + r;

    r = $distr;
    a2 = a2 + r;
    a5 = a5 + r;

    return (a1, a2, a3, a4, a5);
  }
  
}.

masking sni 4 R.refresh5 Byte.ComRing.(+). 


masking sni 5 R.refresh6 Byte.ComRing.(+).

